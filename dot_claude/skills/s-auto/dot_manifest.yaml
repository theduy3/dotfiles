# Distillation provenance — consumed by /update-distill
artifact: s-auto
# local-addition 2026-08-10: Halt Protocol §5 gained a publish step (Artifact on
# ~/tasks/.s-run/<slug>.md, redacted, before the PushNotification so the ping carries a URL),
# and the Run-State frontmatter gained an `artifact:` key. No upstream Source — do not
# attribute it to one on drift. NOTE: this edit invalidates the s-auto hash pinned by
# s4-review/.manifest.yaml; that drift is expected and carries nothing to mirror.
type: skill
distilled: 2026-08-08
spec: ~/tasks/spec-s-star.md
sources:
  - path: ~/.claude/gsd-core/workflows/debug.md
    hash: 68acb5badf94a13008eb28e6b05b9ec2ed0422c2a52befdb7efa6ca9e27f1b18
    removed: >-
      gsd-uninstalled 2026-08-07 — file survives only in
      ~/.claude/.gsd-removal-backup-20260807/ which is temporary; deliberately NOT remapped
      there (a dated backup path is not durable provenance). Row is historical: the
      distillate is self-contained and unaffected.
    took: >-
      Persistent-state model for the Run-State File: slug file with frontmatter
      status fields, Current Focus block (stage + next_action), timestamped
      Evidence log, resume-by-reading-the-file (the file IS the context),
      DATA_START/DATA_END bounding of untrusted pasted content, slug
      sanitization regex. Changed: location ~/tasks/.s-run/ (home dir — guard-
      hook and worktree-removal immunity, per review amendment); orchestrator
      is sole writer; double-merge guard on status: merged.
  - path: ~/.claude/rules/common/worktree-safety.md
    hash: 4068e264583e40379f456dfe0cffdaf873ad6ff1b3accc62b719ae56e0da69fa
    took: >-
      Parent-side CWD-ENOENT ordering contract verbatim as Cleanup steps 1-4
      (enumerate → ExitWorktree in parent → verify pwd = main root → only then
      remove); one-Enter-per-run cache-bust budget. 2026-08-08 refresh: the
      harvested regions above were UNCHANGED; what drifted was the guards section.
      Took one line of it into §2 step 3 — the 2026-08-07 audit found
      worktree-branch-guard.js exceeded its timeout on 2 of 2 runs and had never
      once completed, so the claim lock is now documented as a backstop never
      relied on rather than as enforcement, aligning /s-auto with s-implementer's
      existing "guards are fail-open; be correct, don't rely on them" posture.
      Deliberately NOT taken: the guards inventory itself (which hook exists, its
      timeout value, the deleted gsd-phase-worktree-guard) — that is settings
      state rather than stage doctrine, and would rot in a skill file.
notes: >-
  The stage flow, panel composition + conditional triggers, fix-loop cap 2,
  5-condition halt surface, PushNotification ping, and Seam-ownership
  description clause come from the spec + ADRs 0001/0003/0004/0006/0007, not
  from Sources. Stage content lives in the owned s-* agents (see their
  manifests); this skill is orchestration only, by design.
