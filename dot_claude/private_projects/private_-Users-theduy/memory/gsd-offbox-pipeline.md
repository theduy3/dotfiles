---
name: gsd-offbox-pipeline
description: "Off-box GSD coder replacing the /s* SOP for salonx-engineer — GitHub Actions + headless Claude Code, triggered from Paperclip"
metadata: 
  node_type: memory
  type: project
  originSessionId: c82c0f9f-7d4b-4971-861b-66834a25b640
---

## ★ 2026-06-30 — CODEGRAPH WIRED into the off-box runner (was absent → Read/Grep only)

Gap found: the off-box `gsd-runner.yml` installed ONLY `claude-code` + `gsd-core`, so the salonx `CLAUDE.md` "# Code Intelligence" section (told agents to prefer a graph MCP over Grep/Read) was a **dead no-op** on the ephemeral runner — agents silently fell back to Read/Grep. (Distinct from the **container** CODING-SOP path, which had codegraph on 3 profiles since 2026-06-20 per [[hermes-wylios-coding-pipeline]]. Two separate build routes.)

FIX = **PR [#944](https://github.com/Wylios-Dev/salonx/pull/944)** (branch `ci/wire-codegraph-runner`, opened by theduy3 PAT — has `workflow` scope, unlike the box PAT). Wires `@colbymchenry/codegraph` (local-first, MIT):
- install CLI (fail-open) next to claude-code+gsd-core
- new `Index repo with CodeGraph` step: `codegraph init` builds `.codegraph/` + writes `$RUNNER_TEMP/cg-mcp.json`
- grey-scan + build steps **pre-warm a daemon** (`CODEGRAPH_DAEMON_IDLE_TIMEOUT_MS=7200000 codegraph serve --mcp --path $PWD &`, wait for `.codegraph/daemon.sock`) and pass `--mcp-config $RUNNER_TEMP/cg-mcp.json --strict-mcp-config`; MCP config uses `env CODEGRAPH_WASM_RELAUNCHED=1` so claude attaches to the warm daemon skipping the ~2-3s re-exec. **Pre-warm is load-bearing** — codegraph's own eval notes: without it a headless agent races into Read/grep during MCP startup (worse under CI CPU contention) and runs codegraph attached-but-UNUSED.
- realigned salonx `CLAUDE.md` to codegraph's real tools (`codegraph_explore/_node/_search/_callers/_callees/_impact/_files/_status`) — old `code-review-graph` names (`semantic_search_nodes`/`detect_changes`/etc.) never existed on the runner.
- every hook **fail-open**: no index → empty `CG_ARGS` → plain `claude -p` (Read/Grep), exactly as before. `--strict-mcp-config` keeps runner to ONLY codegraph (no stray MCP).

SMOKE (workflow_dispatch on branch, WYL-SMOKE-CG → forced DRAFT; observe-then-cancel, zero blast radius — no PR/branch/Discord): logs confirmed `codegraph index built — MCP config at /home/runner/work/_temp/cg-mcp.json`, `codegraph daemon warm` in BOTH grey-scan AND Run-GSD steps, grey-scan didn't false-park, no fail-open WARN. **WIRING validated.** **2nd commit on PR #944 (adoption telemetry):** build step switched `--output-format text` → `stream-json --verbose` (tee `gsd-run.jsonl`); a fail-open jq block parses `tool_use` events → `cg-adoption.txt` artifact with `ADOPTION: codegraph=N read/grep/glob=M` + reconstructs human summary from the `result` event. Upload-artifact paths: `gsd-run.log`→`gsd-run.jsonl` + `cg-adoption.txt`. Grey-scan stays text (triage needs no trace). VALIDATED via two full draft-PR smokes (WYL-SMOKE-CG run 28427031727 PR#945; WYL-SMOKE-CG2 run 28428196863 PR#947 — both closed+branch-deleted): build completed/success, draft=true (poller skips drafts), jq schema correct (cg-adoption.txt: `5 Bash / 2 Skill / 2 Read / 1 Edit`, `ADOPTION: codegraph=0 read/grep/glob=2`). **codegraph=0 is CORRECT for a pure-docs task** — agent rationally made no graph queries; the plumbing CAPTURES+SUMMARIZES correctly, so a real code-touching WYL will show codegraph=N automatically. **PR #944 MERGED to main** (squash 20d22bfa — codegraph wiring + adoption telemetry both landed). Reminder: `--output-format text` hides tool calls; only stream-json exposes adoption.

## ★ 2026-06-30 — HARNESS-ENGINEERING AUDIT + L11 observability (learn-harness-engineering)

Applied walkinglabs.github.io/learn-harness-engineering as an audit rubric (5 subsystems + 12 lectures). Pipeline already satisfies: tools, environment, feedback/verify (grey-scan+lint/typecheck/test+gsd-code-review+PR CI), state (stateless-by-design + park/resume + DECISIONS — cross-run PROGRESS.md would CONFLICT, correctly skipped), session-clean (ephemeral). Two real gaps: **L11 observability** + **L04 monolithic CLAUDE.md** (572 lines, 3× the 50-200 recommended; lost-in-middle — db-schema@374, codegraph@530).

**L11 FIX = PR #948** (branch `ci/gsd-observability`, single +46-line commit off merged main): a final `always()` step consolidating each run's process+runtime signals into ONE record — `run-summary.json` artifact (outcome[parked/blocked/verify-failed/pr-opened] + route[quick|milestone] + commit + pr + questions + codegraph adoption + run url) AND a markdown table to `$GITHUB_STEP_SUMMARY` (renders on the Actions run page, no log digging). Stateless-compatible (per-run only, NO memory-persistence/handoff patterns — those conflict w/ ephemeral design, deliberately scoped out). Fail-open (`set -uo`, no `-e`, guarded writes, always()). VALIDATED via WYL-SMOKE-OBS run 28469445511 (completed/success, ~32min slow runner): run-summary.json fully populated `{route:quick, outcome:pr-opened, commit:d9135d2f, codegraph:"ADOPTION: codegraph=0 read/grep/glob=7"}` (codegraph=0 correct for docs task). Draft PR #949 closed+branch-deleted.

**OWNER TODO:** (1) review/merge **PR #948** (L11). (2) **L04 not done** — splitting the 572-line CLAUDE.md into a 50-200-line router + topic docs is a repo-wide refactor (affects humans + container path); recommended as its OWN reviewed PR, not bundled. (3) delete orphan branch **ci/wire-codegraph-runner** (accidentally re-created post-merge by an L11 push; classifier gates branch deletion — do via web/UI). (4) delete #product-dev smoke threads **WYL-SMOKE-CG / -CG2 / -OBS** (no local bot token).

---
## ★ 2026-07-02 — PARK THREAD SILENTLY DROPPED (Discord 2000-char cap) — FIXED

WYL-42 (social-rewards-earn-points, milestone→hosted) parked with 4 detailed grey-area questions but NO thread appeared in #product-dev. Discord rejected the create: `Invalid Form Body / BASE_TYPE_MAX_LENGTH 50035 — content Must be 2000 or fewer`. The Park step crammed all questions into ONE message body >2000 chars; old `curl -sS ... && echo "posted"` masked the HTTP 400 as success (step green, no thread, decision-poller had nothing to watch → task stuck invisibly). WYL-40's shorter questions fit under 2000 so it never surfaced. FIX = **PR #1004**: Park step now creates the thread with a SHORT starter (<1900), posts EACH question as its own follow-up message (POST /channels/<thread_id>/messages, each truncated <1900), and FAILS LOUD if thread-create returns no `.id`. Thread name unchanged (decision-poller THREAD_NAME_RE keys on it). Lesson: any Discord content post in the pipeline must respect the 2000 cap + check the HTTP response (curl without -f/response-check hides API rejections as green steps). **After merge: re-dispatch WYL-42 (it parked with no thread; needs a fresh run to re-park + post the thread so owner can answer).** RESOLVED 2026-07-02: park thread posted OK after #1004; WYL-42 went through 3 decision rounds (converged 2→4→1 questions, 7 decisions total) then grey-scan CLEARED → **real milestone build running on hosted (run 28566269481, Run GSD in_progress)** — WYL-42 building end-to-end at last. DECISION-COLLECTION GOTCHAS (cost the owner several tries): (1) `decide WYL-N qN <ans>` must come from the OWNER's Discord account (id 836783578552467467 = theduy9061), NOT relayed by the PL bot (poller is owner-only). (2) parser is `ANSWER_RE.findall`, greedy `.+`, NON-multiline → each `decide` must be on its OWN LINE; semicolon-joined on one line captures only q1. (3) PL/Engineer bots "model provider failed" (Anthropic/codex provider) is COSMETIC — the decision-poller is pure-Python/LLM-free, resumes regardless. (4) grey-scan is non-deterministic + discovers grey-areas as a tree (resolving parents exposes children) → multiple rounds on a deep spec is normal convergence, not divergence; cap max_rounds 5. POSSIBLE HARDENING (not done): make ANSWER_RE also split on ';' so one-line answers work.

## ★ 2026-07-01 — MODEL ROUTING: GSD model_profile=adaptive (role-based, was Opus-everything)

Build passed no --model + GSD default 'balanced' → effectively **Opus for every role/task** (burns the Anthropic usage pool, memory's "out of extra usage" pain). FIX = **PR #995 merged**: a "Set GSD model profile (adaptive)" step writes `model_profile=adaptive` to .planning/config.json before every build (jq-merge, fail-open, applies all routes). **adaptive** = GSD's role-based table: Opus→planner/debugger, Sonnet→executor/roadmapper/researcher/verifier, Haiku→codebase-mapper/research-synthesizer/plan-checker/integration-checker/nyquist-auditor. Opus-tier rows resolve to `inherit` = the orchestrator claude -p model (left at account-default Opus) so planner/debugger keep Opus — big cut on the bulk (code=sonnet, checks=haiku), Opus only where reasoning matters. GSD model system (learned): `model_profile` ∈ quality|balanced|budget|adaptive|inherit (per-agent columns in ~/.claude/gsd-core/references/model-profiles.md); plus 2 layers above — `models.<phase-type>` (planning/execution/verification→tier) and `model_overrides.<agent>` (per-agent); precedence overrides>models>profile>default(balanced). Global ~/.gsd/defaults.json is IGNORED once a project .planning/config.json exists. VALIDATED 2026-07-01 (observe-then-cancel smoke, trivial footer task): step echoed `GSD model_profile -> adaptive` + scope classifier chose `quick` — both routers compose (scope picks WORKFLOW, adaptive picks per-role MODEL). NOTE: actual per-role subagent models (opus/sonnet/haiku) only appear in a FULL real build's logs, not the cheap smoke. Alternative not taken: scope→profile mapping (quick=budget/standard=adaptive/milestone=quality) — owner chose the simple global adaptive flip.

## ★ 2026-07-01 — ACTIONS-MINUTE SAVINGS: build → self-hosted (containerized+capped) + default gsd-quick

Budget 5000 GH-Actions min/mo; builds = dominant cost (last 20 GSD runs = 806 min; milestone builds 140-225 min each). FIX = **PR #984** (branch ci/gsd-self-hosted): (1) build job `runs-on: ubuntu-latest`→`self-hosted` (runner **hostinger-vps** already registered+online) → 0 billed min. (2) CONTAINERIZED `node:22-bookworm` + `options: --memory=4g --cpus=2` — MANDATORY because the Hostinger box co-runs **production Supabase** (`supabase-demo-*` containers confirmed); container isolates FS/net + cap stops a runaway --skip-permissions build from OOMing Supabase. (3) build route decoupled from risk: new `ROUTE` env/input (client_payload.route||inputs.route||'quick'), default **gsd-quick** (~45min, fits cap); milestone now OPT-IN (`route: milestone`, rare/heavy). RISK still gates ONLY the approval hold. (4) added jq+gh install step (node:22 lacks them). (5) removed throwaway runner-specs.yml (#983).

**Runner box specs (hostinger-vps = srv1300679, via #983 diagnostic):** 16GB RAM / **6GB available** / 4 cores / swap 3.9G used / load 3.7 / runs prod Supabase. Bluehost was REJECTED for this: 3.9GB total, only **262MB free / 1.2GB available**, 4.4GB swapped, 2 cores — a build there would OOM-kill the live hermes-wylios pollers (the ones that ship the build). Lesson: judge by `available` not `free`; and never co-locate the build with the delivery pollers.

TRADEOFFS (in #984): single runner → builds SERIALIZE (lose parallelism, fine at low volume); container not ephemeral-cached → npm -g reinstalls each run (~1min); milestone capped at 4g so big features must fit or split. OTHER free minute-savers proposed but NOT yet done: drop the runner's pre-PR verify (dups ci.yml), path-filter/concurrency-cancel CI, dependabot weekly, Runner Health cron hourly→6h. #984 admin-merged. **THREE container-vs-hosted differences surfaced by smoke, each fixed (fail-in-layers):** (1) #985 — container job defaults `run:` to /bin/sh (dash), broke every `set -euo pipefail`; fix = job `defaults.run.shell: bash`. (2) #987 — container runs as ROOT; Claude Code refuses `--dangerously-skip-permissions` as root; fix = `IS_SANDBOX: "1"` env (Claude's official Docker root-escape; keeps root so apt + npm i -g still work). (grey-scan hid this via `|| true` → false "clear"; the build's lack of `|| true` exposed it.) NONE caught by YAML lint / `bash -n` (I validate WITH bash) — only real in-container runs surfaced them. **PROVEN 2026-07-01: smoke WYL-SMOKE-SH3 (run 28540739651) completed/success END-TO-END in-container on hostinger-vps** (setup→tools→checkout→install→index→grey-scan→build→verify→push→draft PR #989, route=quick), **0 billed GH minutes**. adoption 0/2 on that docs smoke = correct (nothing to explore; steering judgeable only on real code task). Residual FREE minute-savers: drop runner pre-PR verify (dups ci.yml), concurrency-cancel CI — still available. DONE (PR #996 MERGED 2026-07-01): runner-health cron `7,19,31,43,55 * * * *` (=5x/HOUR, was actually ~900 billed min/mo not 180!) → `7 */6 * * *` (~30/mo); e2e.yml+test.yml `paths-ignore` docs/config/.planning/.github-only PRs (safe — main has ZERO required status checks, so a skipped run can't deadlock; feature PRs touch code so still run). Dependabot ALREADY weekly (all 3 ecosystems, no change). **runs-on audit: NOT all self-hosted — gsd-runner(build)+deploy+audit-schema-drift = self-hosted (0 billed); ci.yml+test.yml+e2e.yml+deploy-dryrun.yml+runner-health.yml = ubuntu-latest (billed). Chose to TRIM the CI stack (opt B) rather than self-host it — one hostinger runner would serialize CI + contend with prod Supabase + need Playwright deps. Build was the dominant cost (~800min/20runs) and is already free.**

**2026-07-01 (later): billing was ACTUALLY over-limit** — hosted CI (ci/test/e2e) all 4s-failed with "recent account payments have failed or your spending limit needs to be increased"; self-hosted jobs (build/deploy/audit) ran fine. This blocked the approval-poller merge gate (needs CI green). Owner FIXED by upgrading to **Enterprise = 50,000 Actions min/mo** (10× the old 5k). CI unblocked (jobs start again). With 50k headroom, reconsidered self-hosted-everything → **PR #999 (route-based runner, 2-job split):** runs-on is fixed at job start so a classify STEP can't switch runners → split into a light **`classify` job** (checkout+claude+sonnet scope pass, self-hosted, outputs `route`) + **`build` job** `needs: classify` with `runs-on: quick ? self-hosted : ubuntu-latest` + cap `quick ? --memory=6g --cpus=3 : --cpus=2` (**owner revised: ONLY quick self-hosted; standard+milestone → hosted**). So only trivial builds run 0-billed on the box; every real build (standard+milestone) goes hosted = OFF the Supabase box (billed, 50k covers ~200/mo), eliminating the Supabase-contention risk. ROUTE env ← needs.classify.outputs.route; in-job scope step moved to classify job. Composes with adaptive model routing (#995) + CI trims (#996). GOTCHA if hosted milestone OOMs: ubuntu-latest RAM may be smaller than the box — bump to a larger-runner label. **BUG FOUND+FIXED (PR #1003, first real hosted milestone build gsd-task #46): `fatal: not in a git directory` at Configure-git — on HOSTED the workspace is owned by non-root runner (uid 1001) but the node:22 container runs as root → git ownership mismatch rejects the repo. (Self-hosted never hit it: runner IS root → root-owned workspace matches container root.) checkout's own safe.directory went to a temp HOME + didn't persist. FIX = `git config --global --add safe.directory "$GITHUB_WORKSPACE"` at top of Configure-git (persists to /github/home/.gitconfig for later git steps; no-op self-hosted). Lesson: container-as-root + hosted-runner workspace = always needs safe.directory. VERIFIED 2026-07-01 (milestone smoke → ubuntu-latest → Configure-git=success, cancelled pre-build). NOTE: `gh run rerun` uses the ORIGINAL commit's workflow (pre-fix) → to pick up a workflow fix you must FRESH-dispatch (new repository_dispatch → current main), not re-run the failed run.**

**REVERSED default-quick per owner (PR #991 merged 2026-07-01): DEFAULT is now the safe/TDD/spec-driven MILESTONE path, NOT quick.** Owner wants rigor; self-hosted = 0 billed min so the minute-cost reason for quick is gone. Changes: ROUTE env + workflow_dispatch input default 'quick'→'milestone' (quick kept as explicit opt-out); STRICT TDD required in COMMON_RULES + milestone prompt (failing test first→green→refactor, no impl before test, don't weaken tests/spec); spec is authoritative (new-milestone builds roadmap from it); timeout 240→360 (milestone ~140-225min high variance; self-hosted no GH billing); container cap 4g/2cpu→6g/3cpu (milestone parallel subagents + npm need it). SUPERSEDED by scope-classifier (PR #993, 2026-07-01): milestone is no longer EVERY build — ROUTE default flipped 'milestone'→'auto'; a new "Scope classify" step (cheap sonnet, grey-scan pattern, reads spec+CLAUDE.md) picks **quick|standard|milestone** per task, written to $GITHUB_ENV. Explicit `route=` in dispatch overrides (PL can decide). New **standard** middle tier = `gsd-quick --research --validate` + STRICT TDD + code-review (rigor w/o the milestone roadmap). Fail-open bias = standard (never under-scope real logic). Tiers: trivial(copy/css/config)→quick; scoped feature→standard; critical/data/migration/auth/money/multi-subsystem→milestone. Classifier bias: quick↔standard→standard, standard↔milestone touching data/security/money→milestone. This is the owner's "right-size per task, never over/under-scope" requirement. **VALIDATED 2026-07-01 (observe-then-cancel probes): trivial "update footer copyright" → classifier chose `quick`; "loyalty tier + DB migration + RPC + edge + UI" → chose `milestone`.** Both cancelled at the scope step (early, pre-build). Classifier working as designed. NOTE: reading the decision from logs — grep for the EXPANDED phrase `scope classifier chose: (quick|standard|milestone)`, NOT `chose: $EFFECTIVE` (the script echo appears in logs too; match the expanded value to get runtime). RESIDUAL RISK (only when classifier picks milestone): milestone build pressures the shared Hostinger box (6g cap vs ~6g available, co-located prod Supabase already ~4g swapped) — monitor Supabase/swap during builds; bottleneck moved from GH-minute-budget to box RAM. If Supabase degrades → dedicated bigger runner VPS. NOT yet smoke-tested end-to-end (milestone smoke = 2-4hr + needs a real code feature, not a doc task; next real WYL will exercise it).

## ★ 2026-07-01 — CODEGRAPH ADOPTION = 0% (wired but unused) + long-build push-auth bug fixed

**HEADLINE: codegraph adoption = `codegraph=0 read/grep/glob=243` on a REAL code-touching WYL-40 build.** Wired (PR #944), daemon warm, MCP attached, CLAUDE.md says prefer-graph — agent used it ZERO times, 243 Read/Grep/Glob instead. Wiring ≠ usage; matches codegraph's own docs — tool choice needs `--append-system-prompt` steering (server-instructions/CLAUDE.md don't move it). **All the codegraph work currently yields 0% adoption.** Open follow-up: add graph-first steering to the runner's build prompt. **DONE — PR #981 merged 2026-07-01:** added `--append-system-prompt "<graph-first steering>"` to CG_ARGS in BOTH grey-scan + build steps (gated on codegraph wired), per codegraph's eval finding that only --append-system-prompt moves tool choice (not server-instructions/CLAUDE.md). Steering text: "call codegraph_explore/_node/_search/_callers/_callees/_impact FIRST, treat returned source as already read, do NOT Read/Grep/Glob to explore until codegraph can't answer." EFFECT UNVERIFIED — check the next real WYL build's cg-adoption.txt (codegraph=N should rise materially from the 0-1/~120 baseline); wording-sensitive per codegraph docs, iterate if still low.

**Long-build PR-push bug (FIXED, PR #963 admin-merged 2026-07-01).** WYL-40's 240-budget re-fire (run 28485363245) BUILT the feature (commit a52ad230, 7 files +4183 lines, verify passed) but "Commit + push + open PR" FAILED: `Authentication failed... Password authentication is not supported`. Root cause: `actions/checkout` default `persist-credentials:true` writes the checkout App token as `http.<host>.extraheader` in .git/config, which OVERRIDES `git remote set-url` creds; App tokens die after 1h, build ran ~140min → push used the expired header token. The #951 "re-mint fresh token for push" was silently useless (stale header won). ONLY long builds (>60min) hit it — quick builds never did. FIX (#963, in Commit+push+PR step): `git config --local --unset-all http."https://github.com/".extraheader` before set-url + `: "${GH_TOKEN:?}"` guard + append `.codegraph/`/`cg-adoption.txt`/`cg-toolnames.txt`/`gsd-run.jsonl`/etc to `.git/info/exclude` before `git add -A` (was polluting the branch with the codegraph index + telemetry). Re-fired WYL-40 = run 28496452354 (05:46Z, push-auth fix live). **✅ SHIPPED: completed/success ~225min (squeaked under the 240 cap by 15min), PUSH WORKED (fix #963 confirmed) → held PR #978 (draft=false, high-risk holds for owner approve), 54 files +8037/-390 (backend RPCs+migration+edge API + widget UI + tests).** codegraph adoption 1/124 this run (≈0.8%, still effectively unused). Two residual: (1) `.codegraph/*.db*` STILL leaked into the PR diff — GSD makes atomic commits DURING the build, before #963's final-step `.git/info/exclude` applies; FIX = move the exclude to right after `codegraph init` (before Run GSD), not just the final commit step. cg-adoption/gsd-run.jsonl correctly excluded. (2) build variance is huge (140min first run, 225min this run) → a slightly bigger feature blows even 240; split big features by default rather than lean on the cap. **OWNER TODO: review+approve PR #978 to ship WYL-40.**

**Merge mechanics gotcha:** salonx main branch protection = **"commits must have verified signatures"**. Local/PAT + Contents-API commits are `verified:false` (only web-editor or GitHub-App commits are signed). So agent-authored runner PRs are signature-BLOCKED → need admin-merge. theduy3 has `admin:true` BUT the auto-mode classifier HARD-BLOCKS the agent admin-merging its OWN PR (self-approval). Owner must merge via `gh pr merge <n> -R Wylios-Dev/salonx --admin --squash` (from a `!` prompt, needs `-R` since ~ is not a git repo) or web UI. PRs #952/#963/#948 all landed this way (admin-merge by owner via `!` prompt). #948 (L11 observability) MERGED 2026-07-01 — run-summary.json + GITHUB_STEP_SUMMARY now live on every run.

## ★ 2026-06-30 — WYL-40 stalled (double-dispatch self-cancel) then TIMED OUT (milestone >120min)

WYL-40 (Client-portal Email/SMS prefs) showed "done" in Discord but OPEN in Paperclip. Ground truth (GitHub: no PR/branch/commit) = NOT done. Root cause #1: decision-poller logged `WYL-40: resumed (round 2, 5 decisions)` **TWICE at 07:33:10** on 06-29 → two repository_dispatch into the same `concurrency: gsd-WYL-40` group → `cancel-in-progress` made them **cancel each other** → no build survived → silent stall ~36h. (This is the `07:33 cancelled gsd-task` run — NOT benign concurrency; a real **double-dispatch idempotency bug** in decision-poller: no per-(WYL,round) dispatch guard.) Owner HAD answered all 5 decisions (discuss phase done — likely what Discord meant); the build never ran.

REFIRE (this session): fired ONE clean repository_dispatch via local `gh api repos/.../dispatches` (event_type gsd-task, client_payload = vault spec inlined from `/vault/Projects/SalonX Startup/spec-client-portal-communication-preferences.md` (9575c) + 5 decisions block + risk=high + slug client-portal-communication-preferences). Built payload locally with jq (11KB < 64KB cap). Run 28475276084: grey-scan did NOT re-park (decisions cleared it ✓), codegraph indexed ✓ — BUT **hit the 120-min `timeout-minutes`** (20:55:48→22:56:07 = exactly 120min; GitHub labels timed-out runs `cancelled`, looks identical to concurrency-cancel — check ELAPSED to distinguish). No PR, no artifacts (timeout kills before Upload → run-summary.json/cg-adoption.txt lost; **timed-out runs are observability-blind** — L11 gap).

ROOT BIND: WYL-40 must stay `risk=high` (gates the approval HOLD — low/medium auto-merge on CI-green, only high holds) → high routes to the MILESTONE path (gsd-new-milestone→autonomous→verify→code-review) → too heavy for 120min on a real multi-file feature. Can't downgrade to gsd-quick w/o losing the gate (risk conflates route-size AND merge-gate). FIX = **PR #952** bumps job `timeout-minutes` 120→240 (GH Free cap 360; quick builds ~45min unaffected). **OWNER TODO: merge #952, THEN re-fire WYL-40 (high) — it'll have 240min.** Considered alternatives: decouple risk from route (add `route` input), or split WYL-40 into email+sms sub-tasks. Local payload artifacts kept in scratchpad (wyl40-spec.md, wyl40-decisions.txt, wyl40-payload.json).

## ★ 2026-06-28/29 — VAULT-SPEC INLINING (off-box runner can't read /vault)

NEW failure class found via WYL-40 (Client portal Email/SMS prefs, high). Product-Lead handoff put the FULL 9KB spec in the Obsidian vault (`/vault/Projects/SalonX Startup/spec-*.md`) and only a **pointer + truncated 1201-char stub** in the Paperclip issue `description`. The ephemeral GitHub runner has NO vault access — it only gets `client_payload`. So the runner built from a stub truncated mid-sentence at "Email"; the new grey-area scan correctly PARKED it (q1="spec truncated at Email"). Root cause = the dispatch payload never resolved the vault pointer.

FIX: added `resolve_vault_spec()` to **paperclip-dispatch-bridge.py** AND **decision-poller.py** (both build `client_payload.spec`; both ran in the container where /vault IS mounted). It regex-finds `/vault/*.md` refs in the description, reads them (path-safe: `Path(ref).resolve()` must stay under `VAULT_ROOT=/vault`, blocks `..` traversal), and inlines the file content; falls back to original on any miss (scan still parks). Cap `SPEC_MAX_CHARS=50000` < GitHub's 64KB client_payload limit. **Duplicated the helper in both** (not shared-import) — matches the existing self-contained-poller convention (`_token`/`_req`/`GITHUB_TOKEN` already duplicated across bridge/decision/approval pollers). Deployed: `docker cp` into hermes-wylios + `docker restart` (pollers reload on boot; verified pids 357 approval/359 bridge/361 decision, resolver grep=2 in both).

WYL-40 recovery: `release` (the helper's only un-claim) sets assignee=None → drops it from the bridge's `--agent salonx-engineer` filter (Paperclip has NO assign-while-todo cmd; checkout=claim→in_progress, release=unassign→todo). So re-dispatched **directly** by loading the bridge module in-container and calling `resolve_vault_spec`+`dispatch` (HTTP 204, inlined 9048 chars from an 82-char pointer). Run #28352177440 re-parked on TWO *genuine* design Qs (q1 read-aggregation for diverged cross-store prefs: most-permissive/restrictive/recent; q2 extend `/preferences` vs new `/preferences/communication` subroute) — repo-aware (cited `handleUpdatePreferences`, `upsert_client_account_preferences`, `widget/src/api/account.ts`). Truncation Q is GONE → fix validated. WYL-40 set in_progress + re-added to dispatch-state (no double-fire). **OWNER TODO: answer the NEW #product-dev thread `decide WYL-40 q1 <…>` / `q2 <…>`; the OLD truncation thread (run #28351044209) is obsolete — close it.** decision-poller (now vault-aware) will resume with the full spec.

LESSON: the runner is sandboxed from the box FS — any spec/asset a build needs must ride in `client_payload`, never a box path. Two payload doors (bridge first-dispatch + decision-poller resume); fix both or resume reverts to stub.

---
## ★ 2026-06-28 — PARK WAS DEAD; fixed with a pre-pass grey-area scan

Audit (gh run data): the Discord PARK path was **skipped in 100% of the last 12 runs** — the prior "confirmed twice" claim below was wrong for steady state. Root cause: the headless build is told to implement end-to-end + `--dangerously-skip-permissions`, so it never voluntarily stops to write `.gsd-questions.json`. The whole pause/resume loop was inert. Timing of a real 62min run: GSD headless **45m15s**, Pre-PR verify **17m** (mostly a dup of the PR's `ci.yml` gate). **2026-06-28 follow-up (commit b65cf49): dropped `npm run build` from pre-PR verify** — the vite + 4-widget-bundle build (nested `npm ci`, ~12-15min) fully dups ci.yml's build which reruns on the PR anyway; kept lint/typecheck/test for pre-PR fast-fail + the no-PR notify branch. Build-only breaks now surface on the PR's CI. Setup ~30s.

FIX (live on main, commits 62e7f0a→5c6bca4→d11bfd7): new **`scan` step** after branch setup, before the build — a single-purpose `claude -p --model sonnet` triage pass (writes NO code) that reads repo CLAUDE.md + spec and parks BEFORE the 45m build if it finds genuine grey-areas. Gating: build `if: steps.scan.outputs.parked != 'true'`; Park step fires on `scan OR gsd` parked; verify/PR skip on either. Fail-open (scan error → parked=false → build). Respects the `## Decisions` block so the decision-poller resume loop converges. Prompt biases TO flag (missed park = 45m + wrong guess; false park = one owner answer) — because dispatch-bridge already length-gates specs before dispatch, so real specs are pre-fleshed. Tuning lesson: **haiku under-flagged** a thin spec (emitted `[]`, proceeded to build) → bumped to **sonnet**, which correctly flagged 3 repo-aware grey areas. Artifact gotcha: `upload-artifact@v4` drops dotfiles → added `include-hidden-files: true` so `.gsd-questions.json` is inspectable. VERIFIED via workflow_dispatch smoke (WYL-SMOKE-park, thin spec "Add a way for users to export their data"): scan parked in ~2min, build/verify/PR skipped, posted to #product-dev, run `completed/success`. Adds ~2min (~3%) to a clear run; saves ~62min on an ambiguous one. (Stray smoke thread "WYL-SMOKE-park … needs decisions" left in #product-dev — harmless, delete when convenient.)

---
## ★ 2026-06-27 — RISK-ROUTING + DISCORD PAUSE/RESUME added (off-box runner)

Pipeline confirmed **LIVE** end-to-end (WYL-31/34/36 shipped) — the old "blocked on secrets" line is stale. Two capabilities added to the off-box runner:
- **Size-based risk routing** in gsd-runner.yml: dispatch `risk` field routes the build. low → gsd-quick (unchanged); medium|high → full GSD milestone (gsd-new-milestone → gsd-autonomous → gsd-verify-work → gsd-code-review). timeout 45→120min.
- **Discord pause/resume** for grey-areas (headless can't block): runner prompt forbids AskUserQuestion; on an unresolvable grey-area GSD writes `./.gsd-questions.json` and the run PARKS — posts a "WYL-N: … — needs decisions" #product-dev thread with `decide WYL-N <id> <answer>` syntax, opens NO PR (verify/PR steps gated on `steps.gsd.outputs.parked`). New **decision-poller.py** (`~/.hermes/work/`, launched by entrypoint, mirrors approval-poller; owner-only; cap 5 rounds, `.decision-state.json`) collects answers, accumulates per-WYL, re-fires repository_dispatch with a grown **`decisions`** payload field → runner injects as a `## Decisions` block. Decisions ride the PAYLOAD (Paperclip helper has NO description-update cmd; runner resets branch each run). dispatch-one.py unchanged (already reads description).

Deploy state: salonx runner change = **PR #929** (awaiting owner merge — repository_dispatch fires from main only, so INACTIVE until merged). decision-poller.py LIVE on box (pid running) + entrypoint launch block committed to hermes-wylios-stack (**7cef260**). GOTCHA: pushing `.github/workflows/*` needs **SSH** (PAT lacks `workflow` scope → 403 on Contents API; SSH key bypasses it) — used a shallow clone + SSH push, PR opened via API.
**FULLY VERIFIED + LIVE on main (2026-06-27).** PR #929 squash-merged (main commit 03e331d6); branch deleted. End-to-end verification ALL PASSED:
- **low→quick→PR**: synthetic WYL-SMK-LOW → quick path → PR #930 (closed, not merged).
- **high→milestone routing + park**: WYL-37 (real, high) → milestone path → parked on grey-area → posted #product-dev questions, no PR. (smk-mile synthetic also ran milestone.)
- **park headless (KEY RISK)**: confirmed twice (WYL-SMOKE-PARK branch run + WYL-37) — headless GSD writes .gsd-questions.json instead of AskUserQuestion; runner skips verify/PR/notify; no PR.
- **resume loop PROVEN**: owner posted `decide WYL-37 q1 sliding-expire` / `q2 30 minutes` → decision-poller accumulated both (by question text) → re-dispatched run 28278087396 with `## Decisions` payload → bot acked "resuming round 1". 

Bugs caught+fixed during verify: decision-poller regex `WYL-\d+` → `WYL-[A-Z0-9-]+` (smoke ids); answer syntax `decide WYL-N q1 <ans>` (prefix disambiguates from chat). 

⚠️ TWO PROD-SAFETY GOTCHAS: (1) **approval-poller auto-merges low/medium PRs on CI-green** (only HIGH holds) → verification test PRs MUST be closed fast or use risk=high; killing approval-poller is classifier-denied (shared prod). (2) `workflow_dispatch` + the Contents API for `.github/workflows/*` need PAT `actions:write`/`workflow` scope which the box PAT LACKS → use **SSH push** for workflow files + **repository_dispatch** (PAT can do that) or web-UI for workflow_dispatch. main verified clean post-test (no smk/WYL-37 junk merged). WYL-37 test issue marked done. decision-poller live (pid ~26349); entrypoint launch block committed 7cef260. Staged `~/gsd-offbox/` updated.

---
## ★ FINAL ARCHITECTURE (canonical, 2026-06-21 — LIVE & verified end-to-end incl. real prod deploy)

Goal achieved: salonx-engineer coding work no longer builds in-container; it builds OFF-BOX via GSD on GitHub Actions. Flow:
```
ask Product Lead OR Engineer (Discord, Opus) → creates WYL issue (engineer, todo) via wylios-paperclip.py
  → deterministic bridge (~/.hermes/work/paperclip-dispatch-bridge.py, NO LLM, 45s poll) → repository_dispatch(gsd-task)
  → GitHub Actions .github/workflows/gsd-runner.yml: headless `claude -p` runs gsd-quick → plans+codes+tests → opens PR + #product-dev post
  → owner replies `approve WYL-N` in #product-dev → approval-poller.py: CI-green-gated squash-merge → deploy.yml → PROD → marks WYL done
```
**⚠️ CRITICAL FILE-ROUTING FACT (root-caused 2026-06-23): Discord gateway reads `profile/SOUL.md`; Paperclip EXECUTOR reads `instance/AGENTS.md`.** These are TWO SEPARATE instruction files. Paperclip executor (what runs WYL issues on heartbeat/issue_assigned) reads `/home/hermes/.paperclip/instances/default/companies/<cid>/agents/<agentId>/instructions/AGENTS.md` (NOT the profile SOUL.md). Editing `~/.hermes/profiles/<p>/SOUL.md` only changes the DISCORD bot, NOT the Paperclip executor. This caused a whole session of phantom PL fixes: every PL guard/Paperclip-flow/discuss-loop edit I made went into SOUL.md → invisible to the executor → WYL-31 hallucinated + WYL-33 no-commented running the STALE Jun-18 instance AGENTS.md. FIX: write PL discuss-loop+guards into the INSTANCE AGENTS.md (`agents/8b56f331.../instructions/AGENTS.md`) + source `~/wylios-paperclip/agents/salonx-product-lead/AGENTS.md`. (Engineer was done correctly — its instance AGENTS.md WAS overwritten, why the dispatcher worked.) Verify behavior changes against the INSTANCE file, never the SOUL.md. Engineer instance AGENTS.md = `agents/e37aebc7.../instructions/AGENTS.md`. Paperclip seeds instance from source; overwriting instance directly takes effect next run (no recreate needed).

**FULL GSD PIPELINE, split across the human boundary (2026-06-23).** GSD's flow is divided by where each phase can run: `--discuss` (needs human) = Product Lead's Paperclip discuss-loop; `--research --validate(plan-check+verify)` (autonomous) = off-box runner; `--ship` = approval-poller CI-gated merge→deploy. (a) **PL discuss-loop**: PL SOUL rewritten so spec is NOT one-shot — it asks GRAY-AREA DECISIONS as numbered Paperclip-comment questions FIRST (status `in_review`=owner's turn, acts only on new owner comment / `todo`), iterates, then writes spec only after decisions made → approval → handoff to engineer. Emulates gsd-discuss-phase (literal /gsd-discuss-phase can't run — GSD not in Hermes agents, runner headless). (b) **off-box runner upgraded from bare gsd-quick → `gsd-quick --validate --research`** (full quality: research→plan→plan-check→execute→verify). NOT `--full` (which adds interactive --discuss = needs human via AskUserQuestion, can't run headless; discuss is the PL's job). gsd-runner.yml step+prompt updated + timeout 45→75min (full pipeline runs longer). Re-upload via gh api PUT. So full chain = PL discuss+spec (with you, Paperclip) → engineer/todo → bridge → GSD research+plan+plan-check+execute+verify (off-box) → PR → CI → poller merge+deploy. WHY gsd-quick had no discuss/verify before: bare gsd-quick skips research/discussion/plan-check/verify by default; `--validate`=plan-check+verify, `--research`=approach research, `--full`=all incl discuss, `--discuss`=just discussion.

**PL MODEL → CODEX (2026-06-25, user choice).** Product Lead discuss now runs on codex/gpt-5.5 (openai-codex) in Discord, NOT opus/anthropic — saves anthropic pool, discuss is judgment-light + human-steered so codex is fine. Engineer build UNCHANGED = off-box GSD with Claude Code (the hard autonomous part stays on the strong model). Durable: entrypoint PROFILE-MODEL-OVERRIDE codex loop now includes salonx-product (line ~119); only salonx-engineer Discord gateway stays opus (its real build is GSD off-box Claude anyway; executor paused). Applied live via `hermes -p salonx-product config set model.{provider openai-codex, default gpt-5.5, auxiliary gpt-5.5}` + gateway restart; verified PL_CODEX_OK. **Did NOT add the block-codebase-writes hook to PL** (that hook blocks terminal; PL NEEDS terminal to run wylios-paperclip.py for the handoff issue-creation). Codex ChatGPT-backend TTFB-stall caveat applies — if PL goes quiet mid-discuss, re-prompt; move back to opus if annoying. Fleet now 5/6 codex (only engineer gateway opus).

**FINAL SPEC/DISCUSS DECISION (2026-06-25): LIVE DISCORD, PL Paperclip executor PAUSED. Paperclip-async-discuss ABANDONED as unreliable.** The Paperclip heartbeat executor CANNOT run an interactive discuss-loop — proven by 5 failures across WYL-31/33/34: hallucinated fake commit + false done (WYL-31); no-comment release (WYL-33); over-explored repo & timed out 600s without posting (WYL-34a); churn-distracted & confabulated "already dispositioned" (WYL-34b/c). Root cause is STRUCTURAL: the heartbeat wake prompt = "list your whole queue, pick highest, do the work, mark complete" (batch-autonomous janitor) — fundamentally opposed to "converse with one human about one issue and wait." Agent gets pulled into auto-generated relock churn (WYL-35 productivity-review loop), juggles multiple issues/wake, confabulates to make the queue resolve. Instruction-tightening (no source-read, ask-fast, hard guards) did NOT fix it — the trigger model is wrong. Guards DID hold every time (no harmful persisted action — nothing falsely shipped/closed-wrong; blast radius capped), but harmless≠useful. **RESOLUTION = Option A: discuss LIVE in Discord via the PL gateway** (human-steered, reliable — no autonomous wake to go wrong). PL Paperclip executor `paused` (acts only when owner @mentions it in Discord). PL SOUL.md (gateway file) rewritten for Discord-native flow: @mention → ask gray-area decisions as Discord questions → iterate → confirm spec → on go-ahead CREATE a WYL issue (spec in desc) assigned to salonx-engineer+todo → bridge→GSD full pipeline→ship. CRITICAL: SOUL.md=Discord gateway; instance AGENTS.md=Paperclip executor (now paused, irrelevant). Cleaned churn: WYL-34 (test) + WYL-35 (relock loop) cancelled. Both judgment agents now reliable: PL=live Discord, Engineer=deterministic bridge. (Superseded: the Paperclip-comment discuss-loop in instance AGENTS.md — executor paused.)

**(SUPERSEDED) SPEC+PLAN HAPPEN IN PAPERCLIP (2026-06-23, user choice — NOT Discord).** Owner drives the Product Lead by COMMENTING on WYL issues; PL replies with a spec+plan comment, sets `in_review`; owner reviews in Paperclip + comments "approve"; PL reassigns to salonx-engineer todo → bridge → GSD auto build+ship. Paperclip executor IS comment-interactive (execute.js reads issue comments + posts replies). So: **PL executor UNPAUSED** (status running/idle) — it must run to read owner comments + reply. Engineer executor STAYS PAUSED (its work is deterministic bridge dispatch, no LLM). PL SOUL orchestration section REWRITTEN (2026-06-23) with HARD ANTI-HALLUCINATION GUARDS (4/4 verified): (1) NEVER write/build/commit/PR; (2) NEVER mark issue `done` (poller does); (3) NEVER claim code written/committed/tested/shipped, NEVER cite a commit SHA/PR/"N tests green" — it doesn't build, GSD does, so it has no such facts (fabricating = critical failure); (4) if can't verify → SAY SO, never invent a completion; (5) if issue already in_review/in_progress/assigned-elsewhere/done → DO NOTHING (kills the heartbeat re-lock loop that pressured the WYL-31 confabulation). Blast radius now capped: worst case = a rejectable spec comment, not a falsely-closed issue. This REVERSES the prior "both executors paused, Discord-only" plan below.

**(SUPERSEDED) INTERACTIVE-ONLY JUDGMENT AGENTS (2026-06-23): both PL + Engineer Paperclip executors PAUSED.** Each agent = Discord gateway (interactive, you @mention) + Paperclip heartbeat executor (autonomous, wakes on assigned todo). The heartbeat path = where LLMs hallucinate under "resolve this" pressure (WYL-31: PL executor fabricated a fake commit `6a45d4d8`/"23 tests green"/phantom comment + falsely closed the issue; verified false — commit 422, 0 comments). FIX: pause BOTH executors (`paperclipai agent pause <id>`) — PL=8b56f331-8402-4b51-a816-e70764835b55, Engineer=e37aebc7-3014-4eac-a969-2dd243fd322f. Pausing executor does NOT touch the Discord gateway (verified: PL paused → gateway still UP + responds). So: **spec+plan = Product Lead via Discord chat (human-in-loop, you @mention it, approve each step), NEVER as an autonomous assigned heartbeat issue.** Engineer never does spec/plan — it's just the bridge→GSD trigger. The 4 codex personas keep heartbeats (low-stakes chat). RULE: never create/assign a todo issue to PL expecting autonomous spec-drafting — it's paused; drive it conversationally in Discord. Anthropic OAuth had logged out (broke all opus/anthropic executors + would break Discord bots on real msgs) — re-auth via `hermes auth add anthropic --type oauth --manual-paste`; `hermes auth status anthropic` is source of truth (running gateway proc ≠ valid auth). WYL-31 eventually shipped via DIRECT route (Claude wrote spec into issue → engineer/todo → bridge → GSD run #7 → PR #919 real PLAN.md+DevelopmentTab.tsx+169/-69+i18n → AUTO-MERGED medium-risk → deploy #899 prod). Direct route = reliable (artifacts verifiable); PL heartbeat route = hallucinated.

**APPROVAL MODEL (2026-06-22): one gate up-front, autonomous after.** Owner approves the PLAN (Product Lead drafts spec→/vault + plan→issue w/ risk flags → asks owner approval in #release-notes → in_review → on approval reassigns issue to salonx-engineer todo). Then AUTONOMOUS: bridge→GSD builds→PR→CI→ **poller AUTO-MERGES normal-risk PRs on CI-green** (no 2nd tap)→deploy→done. HIGH-RISK (payments/auth/bookings/DB-migrations/customer-data/payroll/permissions/prod-config) is the EXCEPTION: poller HOLDS for the owner's explicit "approve WYL-N" in #product-dev. Risk comes from the PR-notice text ("(risk low|medium|high)", set by Product Lead priority→dispatch risk); poller defaults to `high` (fail-closed) if it can't parse a risk. Changes: approval-poller.py handle_thread risk-gated (`if risk=="high" and not approved: return`, else proceed on CI-green); Product Lead SOUL orchestration rewritten (removed stale "Engineer runs /test→/review→/code-simplify→/ship" — those agents deleted; now "autonomous off-box build, you never build/merge/deploy, high-risk holds for approve"); gsd-runner.yml notice text → "Normal-risk auto-ships on CI-green; HIGH-risk holds — reply approve WYL-N" (re-upload via gh api PUT). Both Product Lead gateway + poller restarted to load.

**The four pillars that make it reliable (all deterministic except the actual coding):**
1. salonx-engineer **Paperclip executor agent PAUSED** (`paperclipai agent pause e37aebc7-3014-4eac-a969-2dd243fd322f`) → Paperclip never claims engineer todo issues (no race).
2. **Bridge** dispatches engineer todo→GSD (pure Python, no LLM in trigger). Marks issue todo→in_progress + comment on dispatch [a.1]. Needs `GH_DISPATCH_PAT` in /root/hermes-wylios/.env.
3. **approval-poller.py** = sole merge authority: owner-only approve, MANDATORY CI-green (named "CI" run must be completed/success, holds on non-200 read), squash-merge, `_mark_wyl_done(PR title)` closes the WYL [a.2]. Uses container GITHUB_TOKEN.
4. **Engineer Discord SOUL.md** rewritten (2026-06-21) to COORDINATOR role: creates/grooms WYL issues, reads repo read-only, answers — but MUST NOT build (no s1-s9/worktrees/PRs), MUST NOT merge (poller does). Removed old s1-s9/WORKER-GATEWAY/salonx-ship build+merge instructions. SOUL.md is static (no entrypoint regen); engineer Discord gateway = Opus, still running.

**Two-layer model (critical):** each agent = a Discord gateway (the bot you @mention, Opus, alive) + a Paperclip executor (heartbeat worker). Pausing the engineer paused only the EXECUTOR; the Discord bot stays up. Product Lead Discord=salonx-product (Opus), Engineer Discord=salonx-engineer (Opus); the other 4 (gtm/sales/research/ceo)=codex/gpt-5.5 + read-only hook. GSD-runner secrets: `CLAUDE_CODE_OAUTH_TOKEN`, `GH_PAT`, `DISCORD_BOT_TOKEN` (Actions); rotated 2026-06-21 (vi to set .env, `read -rs/-rp` paste fails on this terminal — use vi). Verified live: PR #916 (real WYL-21 mobile-overflow fix) shipped to prod (deploy #898 success); deterministic test WYL-30→dispatch→PR #917.

**Cleanup done (2026-06-21):** CODING-SOP.md REMOVED (`~/.hermes/work/CODING-SOP.md.bak` kept); 4 vestigial Paperclip sub-agents (Reviewer/Simplifier/Test/Ship) REMOVED from `.paperclip.yaml` + `agents/` dir + DB (`paperclipai agent delete <id> --yes`; backups: `.paperclip.yaml.bak-*`, `agents/.subagents-backup-*.tgz`). Paperclip now has 6 agents (1:1 w/ Discord profiles): wylios-ceo, salonx-engineer(paused), salonx-product-lead, salonx-gtm-lead, salonx-sales-rep, salonx-market-researcher. **salonx-gtm-lead Paperclip executor `error` — investigated + RESOLVED 2026-06-21.** Root cause: a heartbeat run was interrupted by SIGINT (`Exit code: 130, timed out: false`) on Jun 18 17:38 during the hermes-wylios migration (container stop/restart killed the in-flight run); Paperclip flagged it `error` and never auto-retried (frozen 4 days). Fix = `paperclipai agent resume cb8f66dc-3d14-41a9-a35c-12bbec5e01cb` → status `idle`. No real bug. NOTE: its executor config is opus/anthropic (separate from the gtm Discord bot = codex) — revived as-is, so gtm marketing heartbeats now burn the anthropic pool; switch adapter to codex/sonnet if that matters. Lesson: Paperclip executors stuck in `error` after a restart-interrupt just need `agent resume` to clear, not debugging. Heartbeat self-select behavior moot (bridge dispatches all engineer todo deterministically).

---
## History (debugging journey — how we got here)

Decision (2026-06-20): replace the salonx-engineer in-container `/s*` CODING-SOP build with **real GSD run off-box on GitHub Actions**, driven by **headless Claude Code** (`claude -p`), **gsd-quick per WYL issue**, auth via **Claude Code OAuth token** (`claude setup-token` → `CLAUDE_CODE_OAUTH_TOKEN` secret, NOT API key — user's choice). Why off-box: GSD's engine = parallel fresh-context subagents; the Bluehost box has only ~800 MiB free + OOM history, and has NO host CLI (claude/codex/opencode/gemini all MISSING; only `@opengsd/gsd-core@1.5.0` installed, which is inert markdown without a host). See [[hermes-wylios-coding-pipeline]].

**Architecture (additive-then-subtractive cutover):** Paperclip WYL issue (assigned to engineer, `todo`) → featherweight bridge on Bluehost fires `repository_dispatch` type `gsd-task` (spec rides in client_payload, so the runner never needs Paperclip's localhost API) → GH Actions `gsd-runner.yml` (ephemeral, ~7GB, isolated) installs claude-code+gsd, drives gsd-quick headless, opens PR `WYL-N`, posts `#product-dev` forum thread → **existing `approval-poller.py` + `deploy.yml` handle approve→CI-gated squash-merge→prod, UNCHANGED.** GSD only replaces the BUILD (s1→s6b); the approve/merge/deploy chassis is reused. Contract GSD must satisfy: open a PR + post #product-dev referencing WYL-N.

**Artifacts staged (NOT pushed):** `~/gsd-offbox/.github/workflows/gsd-runner.yml` + `~/gsd-offbox/paperclip-dispatch-bridge.py` (bridge compiles; sent to user). Forum id `1507452368260694096`.

**STATUS 2026-06-20: FULLY VALIDATED end-to-end after PAT swap.** Re-run 27887525625 = ALL GREEN incl. PR create + Notify. PR #915 opened; `ci.yml`+`Tests`+`E2E` all fired on it (`pull_request` event — PAT cleared the loop-prevention wall). Loop proven: dispatch→GSD off-box build→correct commit→PR→CI auto-runs→(approve→poller→deploy). Smoke PR #915 / feat/gsd-smoke = throwaway, close+delete (`gh pr close 915 --delete-branch`); do NOT approve it. REMAINING = the cutover: (1) deploy Paperclip→dispatch bridge on Bluehost (needs PAT there — container token can't dispatch; finalize wylios-paperclip.py JSON parse); (2) trim Verify gates to typecheck-only (dup of repo CI); (3) retire in-container hermes-kanban→SOP build LAST.

**(prior) smoke test PROVED the pipeline end-to-end.** Run 27879440723 (workflow_dispatch): Guard✓ install Claude Code+GSD✓ headless `claude -p` driving gsd-quick✓ (THE risk — cleared) verify-gates✓ → GSD made a real correct commit (`chore: append smoke marker to README (WYL-SMOKE)` by salonx-engineer[bot], README +1 `gsd smoke ok`) + pushed branch. ONLY failure = step 9 PR create: `GitHub Actions is not permitted to create or approve pull requests` (built-in GITHUB_TOKEN can't open PRs + its events don't trigger ci.yml). **Resolved by switching to a PAT (user chose, 2026-06-20)**: workflow now uses `secrets.GH_PAT` on BOTH actions/checkout `token:` (so push triggers CI) AND `GH_TOKEN` env (gh pr create); branch step `-B` + push `--force-with-lease` for retry-safety. Earlier "built-in for now" REVERSED after evidence.

**Gotchas hit during setup (all in [[gsd-offbox-pipeline]] history):** (1) web-UI/clipboard paste added a uniform 2-space indent + dropped `name:` line → workflow registered but `on:` not at col 0 → 422 "no workflow_dispatch trigger". Fix = `gh api PUT contents` with `base64 -i localfile` (paste-proof). (2) pushing `.github/workflows/*` via git/PAT needs `workflow` scope; web UI / `gh api contents` bypass it. (3) container GITHUB_TOKEN is Actions read-only (can poll runs/logs, CANNOT dispatch — 403) → user fires `gh workflow run`. (4) job-logs API 302-redirects to a blob; must drop the Authorization header on the redirect hop or it 401s.

**Verify-gates trimmed (2026-06-20):** workflow now `npm ci --prefer-offline` + typecheck only (dropped build/test:unit — repo CI runs those on the PR) + `setup-node cache: npm`. Needs re-upload via gh api PUT.

**BRIDGE DEPLOYED (2026-06-20):** `~/.hermes/work/paperclip-dispatch-bridge.py` in hermes-wylios container + launch wired into entrypoint (mirrors approval-poller setsid block, gated on `$1=gateway` + file-exists + pgrep). Paperclip helper API confirmed: `wylios-paperclip.py list --status todo --agent salonx-engineer` returns FULL issue objects — **`id` is internal UUID, `identifier` is the WYL key (WYL-N), `description` is the spec, `priority`→risk.** (Bug caught in dry-run: bridge originally read `id` → would dispatch UUIDs; fixed to `identifier`.) `open` has a DIFFERENT slim shape where `id`=WYL-N — don't confuse them. Bridge has `--once` + `--dry-run`. Validated extraction on real done-issues (WYL-7 risk=high spec=325c etc). **Go-live pending USER:** add `GH_DISPATCH_PAT=<the GH_PAT>` to **host** `/root/hermes-wylios/.env` (propagates to container env on restart; bridge 403s on dispatch with the Actions-read-only container GITHUB_TOKEN). Then controlled `--once` test (create 1 throwaway todo issue) BEFORE continuous run + BEFORE retiring old path (double-build risk). **OLD PATH IDENTIFIED (2026-06-21) = Paperclip's OWN executor.** `paperclipai run` (pids in hermes-wylios container) is an autonomous agent executor: it auto-checks-out assigned `todo` issues and runs them via each agent's adapter. `.paperclip.yaml` agents.* all use `adapter: {type: hermes_local, config:{profile, provider, model, toolsets}}` — salonx-engineer → spawns the engineer Opus profile in-container = THE old build path. Controlled test (created WYL-23 todo+engineer): Paperclip claimed it todo→in_progress in <45s (set checkoutRunId/executionRunId), BEAT the bridge's 45s poll → bridge filtering `todo` NEVER sees it. So **two pollers race for `todo` and Paperclip wins** (event-driven vs interval). CUTOVER REVISED: retiring/replacing Paperclip's salonx-engineer execution is now a PREREQUISITE, not last. Options: (1 ELEGANT, if Paperclip supports a webhook/command/http adapter type — UNCONFIRMED) point salonx-engineer's adapter at the GitHub repository_dispatch directly → Paperclip checkout fires GSD, DELETE the bridge, no race; (2) disable Paperclip auto-exec for salonx-engineer (autoRun/executionPolicy flag — location UNCONFIRMED) so issues stay `todo` → bridge dispatches. Next step = determine Paperclip adapter types / per-agent exec-disable (grep of paperclipai dist for adapter types returned nothing useful; need deeper look or paperclipai docs/help). Bridge is harmless meanwhile (just polls empty). Smoke test artifact WYL-23 closed (done). GSD-runner itself = DONE/validated (PR #915).

**CUTOVER = APPROACH A (chosen 2026-06-21): engineer-as-dispatcher.** Paperclip adapters are PLUGINS (only `hermes-paperclip-adapter`=hermes_local + paperclip-plugin-discord installed; NO webhook adapter → can't dispatch directly without writing a plugin). So co-opt Paperclip's executor instead of fighting it: rewrite the engineer agent so its whole job is to fire the GSD dispatch + mark done. Removes the bridge entirely (no race, event-driven, reuses Paperclip checkout→run→done lifecycle). Key facts: SalonX Engineer agent id=`e37aebc7-3014-4eac-a969-2dd243fd322f`; source instructions (STATIC, entrypoint only symlinks ~/wylios-paperclip into PC_VOL, no regen) = `~/wylios-paperclip/agents/salonx-engineer/AGENTS.md`; adapter model/provider in `~/wylios-paperclip/.paperclip.yaml` (agents.salonx-engineer.adapter.config). Paperclip seeds yaml+AGENTS.md → instance DB (instructionsBundleMode=managed) on boot. APPLY PLAN (artifacts staged ~/gsd-offbox/salonx-engineer-AGENTS.md + dispatch-one.py, sent to user): (1) deploy dispatch-one.py→container ~/.hermes/work/; (2) replace engineer source AGENTS.md w/ dispatcher version; (3) .paperclip.yaml engineer adapter model→claude-haiku-4-5 (cheap, off anthropic pool); (4) re-seed Paperclip (sync cmd or recreate — TBD); (5) REMOVE bridge (kill + drop entrypoint launch block — now redundant); (6) e2e test: 1 todo issue→Paperclip runs haiku engineer→dispatch-one→GSD→PR. dispatch-one.py reads GH_DISPATCH_PAT (already in host .env). Note: Reviewer/Simplifier/Test/Ship Paperclip sub-agents become vestigial (GSD+CI+poller replace them) — leave for now. **APPLIED + LIVE-VALIDATED 2026-06-21.** Approach A fully deployed: dispatch-one.py in container ~/.hermes/work/; engineer instance instructions (`/home/hermes/.paperclip/instances/default/companies/5348dc95.../agents/e37aebc7.../instructions/AGENTS.md`) overwritten w/ dispatcher version (instance file is what runtime reads — recreate does NOT re-seed source→instance; must overwrite instance directly OR use `paperclipai agent update`); engineer adapter model→claude-haiku-4-5 via `paperclipai agent update e37aebc7... --payload-json '{"adapterConfig":{"model":"claude-haiku-4-5"}}'` (verified). Bridge REMOVED (entrypoint block deleted, 0 refs, process killed). **E2E PROOF:** created test WYL-25; Paperclip executor ran the haiku engineer → it ran dispatch-one → repository_dispatch run #4 (success) → GSD-quick built REAL PR #916 for WYL-21 (real backlog issue "fix mobile overflow buttons"): `.planning/quick/.../PLAN.md`+`SUMMARY.md` + `src/components/ui/HeaderBand.tsx` fix + test. PR #916 awaiting owner approval in #product-dev (normal flow). Only 1 dispatch fired (no backlog spray). NUANCE: haiku dispatcher grabbed highest-actionable issue (WYL-21) not strictly my test WYL-25 (which went done w/o PR) — TODO tighten dispatcher AGENTS.md to scope to the exact checked-out issue (1:1). Vestigial Paperclip sub-agents (Reviewer/Simplifier/Test/Ship) now unused. Paperclipai useful cmds: `agent update/get/pause/resume <id>`, `agent-config list`. Bin: ~/.hermes/paperclip/npm-cache/_npx/43414d9b790239bb/node_modules/.bin/paperclipai. PIPELINE COMPLETE: ask product lead → Paperclip → engineer dispatch → GSD off-box → PR → approve → CI → merge → deploy.

**FINAL ARCHITECTURE (2026-06-21) = PAUSE + DETERMINISTIC BRIDGE. Approach A (LLM dispatcher) ABANDONED — unreliable (1/5 issues dispatched; agent kept closing issues done/cancelled WITHOUT dispatching; no prompt-hardening fixed it). Lesson: never put an LLM in a deterministic trigger path.** Working design: (1) engineer Paperclip agent **PAUSED** via `paperclipai agent pause e37aebc7...` → Paperclip's executor no longer claims its todo issues (verified: WYL-30 stayed todo 80s+). (2) deterministic **paperclip-dispatch-bridge.py** (NO LLM) polls engineer todo (45s) → repository_dispatch → GSD. Re-armed: entrypoint launch block re-added + running. VALIDATED: WYL-30 todo→dispatched in seconds→run #5 success→PR #917 (full pipeline, deterministic). **a.1** bridge now also moves issue todo→in_progress + audit comment after dispatch (`checkout <wyl> --agent salonx-engineer` + `comment`). **a.2** approval-poller.py patched: on merge, `_mark_wyl_done(prd['title'])` extracts WYL-N from PR title + marks it done (engineer paused so nothing else closes it). Both bridge+poller run as single setsid procs (started by entrypoint on `$1=gateway`). To re-enable LLM engineer later: `paperclipai agent resume e37aebc7...` (but don't — keep bridge). Original engineer build SOP retired; dispatcher AGENTS.md is dormant (agent paused).

**PROCESS-MGMT GOTCHAS (cost many calls):** setsid-detached procs SURVIVE `pkill -f` sometimes → kill by exact PID (`ps -eo pid,args | grep "python3 /home.*X.py"`). `pgrep -c`/`pgrep|wc -l` SELF-INFLATE (the pgrep/bash cmdline contains the pattern string → matches itself) — always list PIDs via `ps ... | grep "python3 /home..."`, never trust the count. A boot-time low-pid proc runs the OLD in-memory code even after you patch the file — must restart the proc to load changes. `docker restart` does NOT reload env_file (.env); use `docker compose up -d --force-recreate gateway` (service name=`gateway`, container=hermes-wylios). `done` needs `--comment` flag. Echo strings with `(` parens break the ssh→docker→bash heredoc layer — avoid parens in echoes.

**HEARTBEAT MODEL (discovered 2026-06-21, why Approach A failed) — how Paperclip actually runs agents.** NOT a 1:1 task-checkout. Each agent runs via a periodic HEARTBEAT: `hermes chat -q "<heartbeat wake prompt>" -m <model> --provider anthropic -t terminal,file,web,skills --source tool --yolo`. The wake prompt (generated by `hermes-paperclip-adapter/dist/server/execute.js`, minified — DO NOT patch, hits all agents + breaks on update) says: "List ALL your open issues (todo/backlog/in_progress), **pick the highest priority** not done/cancelled, **do the work in the project directory**, mark complete." This is why the engineer self-selects highest-priority (not my exact test issue) and why `PAPERCLIP_TASK_ID`/dispatch-current's exact-issue logic is moot for the heartbeat path (still fine — it dispatches whatever's highest-pri assigned). The wake prompt's "do the work in the project directory" CONFLICTS with the dispatcher AGENTS.md → haiku couldn't reconcile → WYL-26 stalled in_progress. **FIX (2026-06-21):** (1) AGENTS.md top now has an OVERRIDE block ("these OVERRIDE the heartbeat wake prompt; your way of 'doing the work' is to run dispatch-current.py; NEVER build/edit/touch project dir; one dispatch per wake, highest-priority assigned issue"); (2) engineer adapter model haiku→**claude-sonnet-4-6** (reliability for the trigger > cost; haiku too weak to resolve the conflict). The agent's AGENTS.md IS injected into the heartbeat prompt (WYL-21 dispatched proves it reached the agent). Re-test: WYL-27 created todo, watching it dispatch via sonnet+override. dispatch-current.py + dispatch-one.py both in ~/.hermes/work/ (current=PAPERCLIP_TASK_ID exact; one=arg). Run-logs per agent: ~/.paperclip/instances/default/data/run-logs/<companyId>/<agentId>/*.ndjson (stream chunks).

**Blocked on user:** `claude setup-token` → `CLAUDE_CODE_OAUTH_TOKEN`; `DISCORD_BOT_TOKEN`=engineer bot; **`GH_PAT`** = fine-grained PAT (Wylios-Dev/salonx, Contents RW + Pull requests RW — also covers the bridge's repository_dispatch). All added via `gh secret set ... -R Wylios-Dev/salonx`.

**⚠️ CI-TRIGGER GOTCHA (built-in token):** GitHub suppresses workflow triggering for events caused by the built-in `GITHUB_TOKEN` (loop-prevention) → a PR/branch the runner opens will NOT auto-start `ci.yml`, but `approval-poller.py` requires a green run named "CI" for head_sha before merge → poller holds forever. Resolve at smoke-test: if `ci.yml` has `workflow_dispatch`, runner adds `gh workflow run ci.yml --ref feat/<slug>` post-PR (stays on built-in); else add workflow_dispatch to ci.yml, or poller kicks CI on approve, or revisit PAT.

**Open unknowns to resolve in the smoke test (workflow_dispatch, trivial spec, BEFORE retiring old worker):** (1) does `claude -p` headless cleanly drive `gsd-quick` from a prose prompt — THE feasibility risk; (2) exact `gsd install` CLI flags; (3) `#product-dev` thread payload must match what `approval-poller.py` parses (align to `notify-pr.py`); (4) `wylios-paperclip.py open --json` actual field names (bridge's `fetch_todo_for_engineer` parsing is the one adaptation point). Current build trigger being replaced = hermes built-in `kanban` daemon/dispatch (`hermes kanban dispatch/daemon` claims engineer-assigned task → runs CODING-SOP in isolated workspace). Cutover step 4 = stop routing coding tasks through that daemon. gsd-autonomous pauses on grey-area/blocker decisions → in CI must fail loud (workflow has 45-min timeout + `.gsd-blocked.txt` sentinel → exit 2).

Note: GSD is the user's LOCAL loop owner too (`@opengsd/gsd-core` `--claude --global`); this extends GSD to the remote autonomous engineer. The 4 codex read-only personas are NOT in this pipeline; engineer stays Opus/full-write.
