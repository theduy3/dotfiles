---
name: hermes-wylios-coding-pipeline
description: hermes-wylios runs an autonomous coding pipeline (Discord→Paperclip WYL issue→kanban→salonx-engineer→PRs on Wylios-Dev/salonx); driven by CODING-SOP.md; CWD-ENOENT was the
metadata: 
  node_type: memory
  type: project
  originSessionId: 35b95f60-5b5d-46eb-9d7e-9be41c0e4681
---

Audited 2026-06-20. The salonx-engineer Hermes profile runs a real autonomous s1→s9 coding pipeline — a port of theduy's local `/s0`–`/s9` Claude Code worktree workflow.

**Flow:** Discord #s0 brainstorm → **Paperclip** WYL issue (the approved spec; Paperclip = issue tracker, embedded-PG :54329, NOT the executor) → **kanban** task (board `default`, assignee `salonx-engineer`) → gateway dispatcher (`dispatch_in_gateway: true`, 60s) claims → engineer follows **`~/.hermes/work/CODING-SOP.md`** → worktree under `~/.hermes/work/wt/<slug>` off `origin/main` on repo `~/.hermes/work/salonx` (github.com/Wylios-Dev/salonx) → TDD build → verify → PR via GitHub REST API (gh NOT installed) → announces PR as Discord forum post in #product-dev → `approval_mode: ask` (HERMES_APPROVAL_MODE=ask) → human replies "approve" on Discord → gateway merges (worker never merges/waits/cleans). Proven: PR #653 opened; real task `wyl-15-ticket-delete-error` worktree exists.

**SOP is a static volume file** (`~/.hermes/work/CODING-SOP.md`, NOT entrypoint-generated; also snapshot-tracked at host `/root/hermes-wylios/docs/CODING-SOP.md`). Edit live copy via `docker exec -u root`; persists. Skills: addyosmani/agent-skills (24, Plan→Build→Verify→Review→Ship map) + obra/Superpowers `verification-before-completion`, mounted as `external_dirs`.

**Fixed 2026-06-20 (opt A):** the #1 failure was `os.getcwd()` FileNotFoundError mid-run — `persistent_shell: true` + worker camping in the kanban scratch workspace (`~/.hermes/kanban/workspaces/t_*`, GC'd by `kanban gc`) or a removed worktree → next terminal command dies before running. Same class as [[feedback_worktree_cwd_enoent]]. Added a `## CWD safety (CRITICAL)` section + re-anchor guard (`cd "$REPO_ROOT" 2>/dev/null || cd ~`) to the SOP. Prior guard existed only at gateway teardown (line ~128), not the worker side.

**Audit findings:** (b) wyl-15 — RESOLVED 2026-06-20: was NOT a stall but an orphan (no kanban task, hand-made June 17) *completed* fix — clean commit `6a8e3db6` "allow deleting tickets with loyalty refs" (supabase migration ON DELETE SET NULL + test), unpushed/no-PR. Removed the dangling worktree keeping the branch (`git worktree remove` from REPO_ROOT keeps branch ref); fix parked on `fix/wyl-15-ticket-delete-error`, recoverable. (c) RESOLVED 2026-06-20: gitnexus deprecated → user on **codegraph** (`@colbymchenry/codegraph@0.9.9`). gitnexus was never in the SOP (one-off in old task body). salonx already indexed (`.codegraph/codegraph.db`, 1542 files/15618 nodes/35291 edges). Installed codegraph in container (volume npm prefix, persists), registered as MCP (8 tools, `cd $REPO_ROOT && codegraph serve --mcp`, watcher ON by default) on **3 profiles: salonx-engineer + wylios-ceo + salonx-product** (NOT all 6 — RAM; MCP spawns lazily per session). `mcp.servers` config survives entrypoint regen (entrypoint only re-sets model.*). SOP updated: `codegraph_explore`/`callers` at s1, `codegraph_impact` blast-radius at Verify. (d) RESOLVED 2026-06-20: `gh` 2.95.0 installed to `/home/hermes/.local/bin` (volume, survives recreate, on agent PATH), auto-auths via env `GITHUB_TOKEN` (fine-grained `github_pat_`, account theduy3) — no `gh auth login`. SOP now uses `gh pr create` / `gh pr merge <n> --squash`; CI gate kept on raw Actions-runs API (Checks API 403 for this token). (e) PASSED + SHIPPED 2026-06-20: first real-code task ran full loop end-to-end in ~6 min → PR #914 → CI all-green (Tests/E2E/CI) → **squash-MERGED to main** (commit d1b45af5, branch deleted, deploy.yml fired). gh auth setup-git run (credential helper durable in volume /home/hermes/.gitconfig → future runs skip push-auth dance). Was: **PR #914 open** (Wylios-Dev/salonx, 5 vitest cases for smsSegmentCount, test-only +46/0 source, typecheck+test+build green, announced #product-dev, not merged — approval gate held). Validated live: CWD-safety (no os.getcwd crash, commands `cd $REPO_ROOT`), codegraph (codegraph_callers used in s1), gh (`gh pr create` worked, self-healed push auth via `credential.helper store`). GOTCHAS for setting up tasks: (1) `kanban create --skill X` HARD-validates skill names → passing a SOP-prose-only name (`github-pr-workflow`) crashes spawn exit-1; use only `hermes skills list` names (test-driven-development, git-workflow-and-versioning, verification-before-completion, code-review-and-quality, ...). (2) `--initial-status blocked` does NOT hold (goes ready→auto-dispatch in 60s); use bare `hermes kanban block <id>` after create, `kanban unblock` to release. (3) git push auth re-derived each run (credential.helper store dance + pending_approval blip) — fix once with `gh auth setup-git` (writes volume .gitconfig). Standing: CA$1 spend cap on engineer's anthropic account — raise it (one heavy user now, since other 5 are codex). See [[hermes-wylios-config-regen]], [[hermes-platform-topology]].
